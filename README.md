./start.sh start       //start httpd service
./start.sh stop        // stop httpd stop
./start.sh status      //look httpd status

cd test_service
cd http_load
make && make install   //using http_load testing this web service


# Httpd
# myhttpd
